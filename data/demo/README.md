# Demo data policy

No persistent demo dataset is committed. The test suite creates clearly labeled synthetic fixtures in a temporary directory and removes them after the run. Synthetic test outputs are software-contract checks and are never manuscript results.

